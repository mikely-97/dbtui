version: 2

models:
  - name: v_a
    description: "Vanilla staging model"
    columns:
      - name: id
        description: "Identifier"
        tests:
          - unique
          - not_null
      - name: name
        description: "Name value"
    config:
      materialized: ephemeral
      tags: ["vanilla", "staging"]
